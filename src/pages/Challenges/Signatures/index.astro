---
import "../../../styles/global.css";
import Menu from "../../../components/Menu.astro";
import Signature from "../../../components/Signature.astro";
import Home from "../../Home/index.astro";
---

<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
    </head>
    <body>
        <div class="container">
            <div class="menu">
                <Menu></Menu>
            </div>
            <div class="sub-container">
                <div class="tasks">
                    <h2>Tareas</h2>
                    <div class="component">
                        <div class="signature">
                            <Signature>
                                <span>Nombre</span>
                                <p>Descripción</p>
                                <span>Hecha</span>
                            </Signature>
                        </div>
                        <div class="signature">
                            <Signature>
                                <span>Nombre</span>
                                <p>Descripción</p>
                                <span>Hecha</span>
                            </Signature>
                        </div>
                        <div class="signature">
                            <Signature>
                                <span>Nombre</span>
                                <p>Descripción</p>
                                <span>Hecha</span>
                            </Signature>
                        </div>
                    </div>
                </div>
                <div class="evaluations">
                    <h2>Evaluaciones</h2>
                </div>
            </div>
        </div>    
    </body>
    <style>
        html, body {
            margin: 0;
            padding: 0;
            height: 100vh;
            width: 100vw;
            overflow: hidden;
            background-color: #FFFFF0;
            /* @apply w-screen h-screen m-0 p-0 overflow-hidden; */
        }

        .container {
            @apply flex h-screen bg-[#FFFFF0] w-full;
        }

        .menu {
            @apply w-[300px] h-full;
        }

        .sub-container{
            @apply w-full bg-[#1aef61] 
        }

        .component{
            @apply bg-[#a968eb] flex justify-around items-center flex-row w-full ;
        }

        .signature{
            @apply bg-[#eb9898]  w-[25%] h-[90%] flex justify-center items-center; 
        }

        .tasks{
            @apply w-full h-[50%] bg-[#07a3eb] flex justify-center  items-center flex-col
        }

        .evaluations{
            @apply w-full h-[50%] bg-[#eb07ba] flex justify-center 
        }
        @media and Screen()



    </style>

</html>

